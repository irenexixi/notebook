一、查询类接口

    传参：非必填字段，没有值的情况，最好不传该字段。前端若传空值，对服务器也是没有影响的，框架可以自动判断空值和null。

二、操作数据库接口，新增或编辑

    传参：新建时表单有的字段，都传递给服务端，没有填写内容的字段，根据字段类型，传递空串 或者 [] 或者 null。
         编辑时表单有的字段，都传递给服务端，没有填写内容的字段或者清空的字段，根据字段类型，传递空串 或者 [] 或者 null。
         编辑时返回的内容，应该是表单需要的所有字段，包括新建时没有填写内容的字段。